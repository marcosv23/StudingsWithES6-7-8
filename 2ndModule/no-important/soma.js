
export default function soma(a,b) // at default export we cannot use '*' to group elements
{
return a+b;

}

export function mult(a,b)// we can use other functions beyond the 'default function'
{
  return a*b
}